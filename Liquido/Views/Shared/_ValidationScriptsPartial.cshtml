@* 
    Included via @section Scripts in views that have forms.
    Enables client-side validation via jquery.validate.
    Scripts are already loaded in _Layout.cshtml so we only
    need the unobtrusive adapter trigger here.
*@
<script>
    $.validator.unobtrusive.parse(document);
</script>